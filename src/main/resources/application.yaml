server:
  port: 8080
  servlet:
    context-path: /identity


spring:
  datasource:
    url: "jdbc:mysql://localhost:3309/identity_service"
    username: root
    password: Keodang123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true